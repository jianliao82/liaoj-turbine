endpoints:
  restart:
    enabled: true
  shutdown:
    enabled: true

logging:
  level:
    root: INFO
    com.netflix.discovery: 'OFF'

eureka:
  client:
    serviceUrl:
      defaultZone: http://192.22.0.6:8765/eureka/,http://192.22.0.7:8765/eureka/

server:
  port: 8077
